# Saccade LLM - 开发日志

## 2026-08-31 — 首屏回到阅读效率

- **定位**：首屏从“不离开原网页”改为“让眼睛更快找到重点”；原因是原页工作只是实现机制，提高阅读效率才是用户价值。
- **演示**：示例正文改为展示主题、判断、因果与寻找重点时间，并减少标题尺寸、取消正文强制两端对齐，让高亮轨道更容易扫读。
- **证据边界**：页面明确区分产品目标与已验证结果；当前已有高亮盲测和位置回归，真实阅读时长与理解率仍需用户实验。

## 2026-08-31 — 发布面与品牌补齐

- **插件资产**：新增“琥珀阅读透镜＋活字墨痕”的栅格 Logo，并生成 Chrome 所需的 16/32/48/128 四档图标；构建检查会阻止图标只存在于源码、没有进入发布包。
- **产品页面**：新增单页发布站，首屏用可交互的原页高亮证明产品，能力、权限、安装方式和 AI 纠错路线均按当前真实状态书写。
- **发布纪律**：算法改进只有通过回归、重新构建 MV3 和真实 Chrome 复验，才算进入插件版本；实验结论不能停留在实验页。

## 2026-08-31 — 独立性收口

- **运行时**：移除第三方阅读高亮服务、环境变量、失败切换和 UI 状态；结构化输出失败现在明确报错。
- **扩展**：候选生成、落点选择和 DOM 渲染均由本项目本地代码完成。
- **仓库**：历史研究材料移出产品树，当前源码和文档只保留独立产品语言。

## 2026-08-31 — 第二轮盲测：旧本地基线下语义排序胜出

- **改变**：候选召回达到 90.6% 后，在全新文章 10 个段落上复测；两侧继续使用同一候选池、密度和视觉样式。
- **观察**：大模型语义排序 8 胜、本地算法 1 胜、都不行 1；但实验页生成后本地选择器又发生升级，因此该结果只适用于本轮页面中的版本。
- **例外**：本地胜出的段落更完整地覆盖了否定与对照；共同失败的段落仍把两条因果链拆成零散词，说明排序和长关系短语仍需修正。
- **决定**：保留大模型语义排序方向，但不据此替最新版本地算法定案；下一轮以最新版选择器为基线复测，再决定插件主路径。

## 2026-08-31 — 产品转向：原页高亮插件

- **入口**：从独立 Web 阅读器转为 Chrome 原网页高亮；原因是主要阅读行为发生在浏览器，切换界面本身增加摩擦。
- **运行方式**：默认改为本地确定性引擎，支持即时开启、完整撤销和最小权限；原因是在线 AI 延迟与稳定性不适合阅读主路径。
- **视觉与选词**：真实页面试用否定了扁平色带和泛词高亮，改为完整字框笔刷、避开原有强调、降低低信息词权重。
- **A/B 实验**：同一真实文章 9 段盲测中，大模型语义排序 4 胜、本地算法 3 胜、都不行 2；共同失败来自 `AI` 等英文术语缺失和中文复合短语拆分。
- **候选召回**：完整短语召回从 21/53（39.6%）升至 48/53（90.6%），已知坏切分降至 0，候选量为原来的 1.73 倍；据此进入全新文章的第二轮同条件盲测。

## [1.0.0] - 2026-06-27

### 功能特性
- **Batch 处理优化**：支持将长文本拆分为多个 batch 并发处理，控制并发请求数量（最多 5 并发），提高处理效率

### 质量保证
- **运行时 QA**：通过 yishuship-qa 流程，完成浏览器探索性测试
  - URL 导入（example.com）正常
  - .txt/.md 文件导入正常
  - 高亮开关切换正常
  - 错误后状态恢复正常
  - 390px 响应式布局无溢出
  - 控制台无阻断性 JS 异常
- **单元测试**：覆盖 batch 处理、结构化输出错误和恢复场景

### 架构设计
- **对抗式设计**：通过独立 peer agent 生成 spec，与 host spec 对比并解决分歧，确保设计的合理性和完整性
- **模块化实现**：独立模块 `llm-client.mjs` 处理 LLM 调用、分批和结构化输出恢复

### 技术细节
- **错误恢复条件**：`isStructuredOutputError` 检测 thinking-only、JSON 解析失败、缺少 HighlightMap 等错误类型
- **Batch 配置**：`MINIMAX_MAX_PARAGRAPHS_PER_REQUEST=4`、`MINIMAX_MAX_CHARS_PER_REQUEST=1200`、并发 5

## [0.9.0] - 2026-06-14

### 功能特性
- 初始 Web MVP 版本
- 支持 URL 和文件导入
- 基于 LLM 的语义高亮生成
- 对照页面（`/compare`）比较参考算法和 LLM 输出
- 长文 batching 支持

### 架构
- 前端：暖纸背景、墨蓝强调、衬线层级、左侧导入控制 + 右侧纸面阅读器
- 后端：Node.js + Express，支持 `/api/health`、`/api/import/url`、`/api/import/file`、`/api/highlight`、`/api/compare`、`/api/experiments` 端点
- LLM 集成：MiniMax-M3（Anthropic 兼容协议），无 key 时退化为本地 mock 算法

### 测试
- 38 个单元测试全部通过
- API smoke 测试覆盖所有端点
- 真实 LLM smoke 测试：12 篇英文 deterministic check 11/12 pass

---

**版本说明**：
- 版本号遵循 [语义化版本控制](https://semver.org/lang/zh-CN/)
- 提交风格遵循 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/v1.0.0/)
- 开发日志基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)
